import { Injectable } from '@nestjs/common';
// 1. Generate with node:crypto [x]
// 2. Check if present. Yes -> re-generate. No - good
@Injectable()
export class LinkGeneratorService {
    // inject campaign repo
}
